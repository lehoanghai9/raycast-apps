{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "keyboard-viewer",
  "title": "Keyboard Viewer",
  "description": "Shows your currently active macOS keyboard layout and what each key types under Shift and Option. Works with any layout, not just one language.",
  "icon": "icon.png",
  "author": "lehoanghai",
  "categories": [
    "Productivity"
  ],
  "license": "MIT",
  "commands": [
    {
      "name": "keyboard-viewer",
      "title": "Keyboard Viewer",
      "subtitle": "Current Layout",
      "description": "Browse your active keyboard layout and see what each key types with Shift / Option.",
      "mode": "view"
    }
  ],
  "dependencies": {
    "@raycast/api": "latest",
    "@raycast/utils": "^1.3.3"
  },
  "devDependencies": {
    "@raycast/eslint-config": "^1.0.6",
    "@types/node": "^22.13.10",
    "@types/react": "^19.0.10",
    "eslint": "^8.51.0",
    "prettier": "^3.0.3",
    "typescript": "^5.2.2"
  },
  "scripts": {
    "build": "ray build -e dist",
    "dev": "ray develop",
    "fix-lint": "ray lint --fix",
    "lint": "ray lint",
    "publish": "ray publish"
  }
}
